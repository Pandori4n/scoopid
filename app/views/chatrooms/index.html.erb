<div class="container">
  <div class="title-container d-flex align-items-center">
    <h3>Messagerie</h3>
  </div>
  <div class="container">
    <div id="fixed-form">
    <%= simple_form_for [@lost_person, @chatroom] do |f| %>
    <%= f.association :volunteer, label: 'Rechercher', prompt: 'Volontaire', label_method: :full_name %>
    <div class= "button-send">
      <%= f.submit "Écrire", class: 'button button-secondary-alt' %>
    </div>
    <% end %>
    </div>
    <div class="new-message title-container d-flex align-items-center"> <h3>🔔 Conversations 🔔</h3></div>
    <div class="bubbles">
      <% @chatrooms.each do |chatroom| %>
        <div class="bubble-container column">
        <%= link_to "#{chatroom.volunteer.full_name}", lost_person_chatroom_path(@lost_person, chatroom), id:"link" %>
        <p><%= chatroom.messages.last.content.first(25) + '...' if chatroom.messages.last %></p>
        </div>
      <% end %>
    </div>
  </div>
</div>
